# Fase 1: base manifest only, no autoscaling/production tuning
# (docs/ARCHITECTURE.md section 11).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: tradingplatform
  labels:
    app: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: tradingplatform/notification-service:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: tradingplatform-env
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: tradingplatform
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - port: 8000
      targetPort: 8000
