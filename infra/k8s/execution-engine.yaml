# Fase 15: modest resource requests/limits on every Deployment; CPU-based
# autoscaling for the stateless services lives in hpa.yaml (see
# docs/SCALABILITY.md for what state lives where).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: execution-engine
  namespace: tradingplatform
  labels:
    app: execution-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: execution-engine
  template:
    metadata:
      labels:
        app: execution-engine
    spec:
      containers:
        - name: execution-engine
          image: tradingplatform/execution-engine:latest
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          envFrom:
            - configMapRef:
                name: tradingplatform-env
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: execution-engine
  namespace: tradingplatform
  labels:
    app: execution-engine
spec:
  selector:
    app: execution-engine
  ports:
    - port: 8000
      targetPort: 8000
