# Fase 1: base manifest only, no autoscaling/production tuning
# (docs/ARCHITECTURE.md section 11).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: paper-trading
  namespace: tradingplatform
  labels:
    app: paper-trading
spec:
  replicas: 1
  selector:
    matchLabels:
      app: paper-trading
  template:
    metadata:
      labels:
        app: paper-trading
    spec:
      containers:
        - name: paper-trading
          image: tradingplatform/paper-trading:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: tradingplatform-env
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: paper-trading
  namespace: tradingplatform
  labels:
    app: paper-trading
spec:
  selector:
    app: paper-trading
  ports:
    - port: 8000
      targetPort: 8000
