# Fase 1: base manifest only, no autoscaling/production tuning
# (docs/ARCHITECTURE.md section 11).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-engine
  namespace: tradingplatform
  labels:
    app: portfolio-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio-engine
  template:
    metadata:
      labels:
        app: portfolio-engine
    spec:
      containers:
        - name: portfolio-engine
          image: tradingplatform/portfolio-engine:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: tradingplatform-env
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: portfolio-engine
  namespace: tradingplatform
  labels:
    app: portfolio-engine
spec:
  selector:
    app: portfolio-engine
  ports:
    - port: 8000
      targetPort: 8000
