# Fase 15: modest resource requests/limits on every Deployment; CPU-based
# autoscaling for the stateless services lives in hpa.yaml (see
# docs/SCALABILITY.md for what state lives where).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: tradingplatform
  labels:
    app: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: tradingplatform/gateway:latest
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          envFrom:
            - configMapRef:
                name: tradingplatform-env
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: tradingplatform
  labels:
    app: gateway
spec:
  selector:
    app: gateway
  ports:
    - port: 8000
      targetPort: 8000
