# Fase 1: base manifest only, no autoscaling/production tuning
# (docs/ARCHITECTURE.md section 11).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backtester
  namespace: tradingplatform
  labels:
    app: backtester
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backtester
  template:
    metadata:
      labels:
        app: backtester
    spec:
      containers:
        - name: backtester
          image: tradingplatform/backtester:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: tradingplatform-env
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: backtester
  namespace: tradingplatform
  labels:
    app: backtester
spec:
  selector:
    app: backtester
  ports:
    - port: 8000
      targetPort: 8000
