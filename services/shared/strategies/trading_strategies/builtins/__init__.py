"""Builtin strategy implementations, one module per category.

Every module holds ``@register_strategy`` classes; ``discover()`` imports
them all. Add new strategies by adding modules here - no central list.
"""
